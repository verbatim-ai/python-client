# coding: utf-8

"""
    Verbatim AI — GenAI Backend API

      ## Concepts API of the **Verbatim AI** Retrieval-Augmented-Generation (RAG) platform is built over 5 domains: - **Corpus** — a knowledge base. Holds documents, threads, and is bound to an embedding model and a summary LLM. - **Document** — a file ingested into a corpus (PDF, DOCX, HTML…). - **Chunk** — one embeddable piece of a document, produced by ingestion. The unit retrieval actually returns. - **Thread** — a conversation thread bound to one or more corpora. - **Post** — a single user query or system answer inside a thread. Answers reference attachments (the chunks used as context).  ## Authentication Two authentication methods are accepted on endpoints:  | Method | Header | Allowed HTTP methods | Use case | |--------|--------|----------------------|----------| | **JWT Bearer** | `Authorization: Bearer <jwt>` | All | Server-to-server calls with your RSA-signed JWT | | **Access Token** | `X-Access-Token: <token>` | **Defined by the scope of the token** | Short-lived tokens issued by `POST /v1/access-token/` |  ## API status Get a fresh status from our [API Status dashboard](https://verbatim-ai.openstatus.dev/). Events, maintenance schedules and incidents will be reported in this page.  ## Conventions - **Pagination** — list endpoints accept `pageSize` (default `25`) and `pageIndex` (default `0`). - **IDs** — all resource identifiers are UUIDv4 strings. - **Timestamps** — ISO-8601 (`2026-04-23T04:06:51Z`). - **Errors** — non-2xx responses return a JSON body matching the `Error` schema. --- 

    The version of the OpenAPI document: v1
    Contact: contact@verbatim-ai.com
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional, Union
from typing_extensions import Annotated
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class AgentCreateRequest(BaseModel):
    """
    Payload to create a custom agent inside your organization.  Only `name` is required. Every other field falls back to a documented default, so the smallest useful body is `{\"name\": \"...\"}` — and each field left out stays tied to the platform default rather than being frozen at today's value.  The created agent is always a custom one: it is attached to the organization carried by your credentials, `lock` is `false` and `default` is `false`. Core agents are seeded by the platform and cannot be created over the API. 
    """ # noqa: E501
    name: Annotated[str, Field(strict=True, max_length=128)] = Field(description="Human-readable name of the agent. Max 128 characters, and unique in the listing you see — a name one of your own agents uses, or one a platform agent (`lock: true`) carries, answers `409`. Compared exactly, so `Support` and `support` are two names and `Verbatim Default v2` is free.", json_schema_extra={"examples": ["Support assistant"]})
    description: Optional[StrictStr] = Field(default=None, description="Free-form description of what this agent is for.", json_schema_extra={"examples": ["Answers customer questions from the support knowledge base, in the customer's language."]})
    top_k: Optional[Annotated[int, Field(strict=True, ge=1)]] = Field(default=None, description="Chunks the vector search returns, before re-ranking narrows them down. Defaults to `5`.", alias="topK", json_schema_extra={"examples": [10]})
    rerank: Optional[StrictBool] = Field(default=None, description="Whether retrieved chunks are re-ranked by an LLM before the answer is generated. Defaults to `true`.", json_schema_extra={"examples": [True]})
    rerank_top_k: Optional[Annotated[int, Field(strict=True, ge=1)]] = Field(default=None, description="Chunks kept after re-ranking. Omit to track the platform default.", alias="rerankTopK", json_schema_extra={"examples": [3]})
    context: Optional[StrictStr] = Field(default=None, description="First third of the system instruction — what the model is looking at. Omit to track the platform default.", json_schema_extra={"examples": ["Use the following document excerpts to answer the user:"]})
    behaviour: Optional[StrictStr] = Field(default=None, description="Second third of the system instruction — how the model should act. Omit to track the platform default.", json_schema_extra={"examples": ["Answer only from the excerpts provided. When they do not contain the answer, say so plainly."]})
    spirit: Optional[StrictStr] = Field(default=None, description="Last third of the system instruction — the tone it should take. Omit to track the platform default.", json_schema_extra={"examples": ["Be precise, concise and factual."]})
    use_history: Optional[StrictBool] = Field(default=None, description="When `false`, previous posts of the session are not replayed. Defaults to `true`.", alias="useHistory", json_schema_extra={"examples": [True]})
    history_size: Optional[Annotated[int, Field(strict=True, ge=1)]] = Field(default=None, description="Number of trailing posts replayed. Omit to replay the whole session.", alias="historySize", json_schema_extra={"examples": [10]})
    thinking_mode: Optional[StrictStr] = Field(default=None, description="Reasoning budget the model spends before answering. Defaults to `HIGH`.", alias="thinkingMode", json_schema_extra={"examples": ["HIGH"]})
    temperature: Optional[Union[Annotated[float, Field(le=1, strict=True, ge=0)], Annotated[int, Field(le=1, strict=True, ge=0)]]] = Field(default=None, description="Sampling temperature, between 0 and 1. Omit to leave the model default untouched.", json_schema_extra={"examples": [0.2]})
    rerank_model: Optional[StrictStr] = Field(default=None, description="Model used for re-ranking. Must be one of the names `GET /v1/config/model` advertises.", alias="rerankModel", json_schema_extra={"examples": ["gemma4"]})
    base_model: Optional[StrictStr] = Field(default=None, description="Model used to generate the answer. Must be one of the names `GET /v1/config/model` advertises. Omit to keep whatever model the session was created with.", alias="baseModel", json_schema_extra={"examples": ["mistral"]})
    __properties: ClassVar[List[str]] = ["name", "description", "topK", "rerank", "rerankTopK", "context", "behaviour", "spirit", "useHistory", "historySize", "thinkingMode", "temperature", "rerankModel", "baseModel"]

    @field_validator('thinking_mode')
    def thinking_mode_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['LOW', 'HIGH']):
            raise ValueError("must be one of enum values ('LOW', 'HIGH')")
        return value

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of AgentCreateRequest from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of AgentCreateRequest from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "name": obj.get("name"),
            "description": obj.get("description"),
            "topK": obj.get("topK"),
            "rerank": obj.get("rerank"),
            "rerankTopK": obj.get("rerankTopK"),
            "context": obj.get("context"),
            "behaviour": obj.get("behaviour"),
            "spirit": obj.get("spirit"),
            "useHistory": obj.get("useHistory"),
            "historySize": obj.get("historySize"),
            "thinkingMode": obj.get("thinkingMode"),
            "temperature": obj.get("temperature"),
            "rerankModel": obj.get("rerankModel"),
            "baseModel": obj.get("baseModel")
        })
        return _obj



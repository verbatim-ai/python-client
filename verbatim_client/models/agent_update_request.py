# coding: utf-8

"""
    Verbatim AI — GenAI Backend API

      ## Concepts API of the **Verbatim AI** Retrieval-Augmented-Generation (RAG) platform is built over 4 domains: - **Corpus** — a knowledge base. Holds documents, sessions, and is bound to an embedding model and a summary LLM. - **Document** — a file ingested into a corpus (PDF, DOCX, HTML…). - **Session** — a conversation thread bound to one or more corpora. - **Post** — a single user query or system answer inside a session. Answers reference attachments (document chunks used as context).  ## Authentication Two authentication methods are accepted on endpoints:  | Method | Header | Allowed HTTP methods | Use case | |--------|--------|----------------------|----------| | **JWT Bearer** | `Authorization: Bearer <jwt>` | All | Server-to-server calls with your RSA-signed JWT | | **Access Token** | `X-Access-Token: <token>` | **Defined by the scope of the token** | Short-lived tokens issued by `POST /v1/access-token/` |  ## API status Get a fresh status from our [API Status dashboard](https://verbatim-ai.openstatus.dev/). Events, maintenance schedules and incidents will be reported in this page.  ## Conventions - **Pagination** — list endpoints accept `pageSize` (default `25`) and `pageIndex` (default `0`). - **IDs** — all resource identifiers are UUIDv4 strings. - **Timestamps** — ISO-8601 (`2026-04-23T04:06:51Z`). - **Errors** — non-2xx responses return a JSON body matching the `Error` schema. --- 

    The version of the OpenAPI document: v1
    Contact: contact@verbatim-ai.com
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional, Union
from typing_extensions import Annotated
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class AgentUpdateRequest(BaseModel):
    """
    Patch a custom agent. Every field is optional; the ones you omit keep their current value.  Because an omitted field means \"leave alone\", it cannot also mean \"put this back to the platform default\". That is what `reset` is for: list the names of the nullable fields you want to un-set, and they go back to tracking the platform default instead of holding the value you gave them earlier. `reset` is applied after the rest of the body, so naming a field in both wins for `reset`.  Core agents (`lock: true`) are not writable — patching one answers `400`. 
    """ # noqa: E501
    name: Optional[Annotated[str, Field(strict=True, max_length=128)]] = Field(default=None, description="Human-readable name of the agent. Max 128 characters. Renaming onto a name another of your agents holds, or one a platform agent (`lock: true`) carries, answers `409`. Re-sending this agent's own current name is not a rename and is never a conflict.", json_schema_extra={"examples": ["Support assistant (v2)"]})
    description: Optional[StrictStr] = Field(default=None, description="Free-form description of what this agent is for.", json_schema_extra={"examples": ["Answers customer questions from the support knowledge base, in the customer's language."]})
    top_k: Optional[Annotated[int, Field(strict=True, ge=1)]] = Field(default=None, description="Chunks the vector search returns, before re-ranking narrows them down.", alias="topK", json_schema_extra={"examples": [8]})
    rerank: Optional[StrictBool] = Field(default=None, description="Whether retrieved chunks are re-ranked by an LLM before the answer is generated.", json_schema_extra={"examples": [False]})
    rerank_top_k: Optional[Annotated[int, Field(strict=True, ge=1)]] = Field(default=None, description="Chunks kept after re-ranking.", alias="rerankTopK", json_schema_extra={"examples": [5]})
    context: Optional[StrictStr] = Field(default=None, description="First third of the system instruction — what the model is looking at.", json_schema_extra={"examples": ["Use the following document excerpts to answer the user:"]})
    behaviour: Optional[StrictStr] = Field(default=None, description="Second third of the system instruction — how the model should act.", json_schema_extra={"examples": ["Answer only from the excerpts provided."]})
    spirit: Optional[StrictStr] = Field(default=None, description="Last third of the system instruction — the tone it should take.", json_schema_extra={"examples": ["Be precise, concise and factual."]})
    use_history: Optional[StrictBool] = Field(default=None, description="When `false`, previous posts of the session are not replayed.", alias="useHistory", json_schema_extra={"examples": [True]})
    history_size: Optional[Annotated[int, Field(strict=True, ge=1)]] = Field(default=None, description="Number of trailing posts replayed.", alias="historySize", json_schema_extra={"examples": [10]})
    thinking_mode: Optional[StrictStr] = Field(default=None, description="Reasoning budget the model spends before answering.", alias="thinkingMode", json_schema_extra={"examples": ["LOW"]})
    temperature: Optional[Union[Annotated[float, Field(le=1, strict=True, ge=0)], Annotated[int, Field(le=1, strict=True, ge=0)]]] = Field(default=None, description="Model temperature. Temperature must match model range.", json_schema_extra={"examples": [0.2]})
    rerank_model: Optional[StrictStr] = Field(default=None, description="Model used for re-ranking. Must be one of the names `GET /v1/config/model` advertises.", alias="rerankModel", json_schema_extra={"examples": ["gemma4"]})
    base_model: Optional[StrictStr] = Field(default=None, description="Model used to generate the answer. Must be one of the names `GET /v1/config/model` advertises.", alias="baseModel", json_schema_extra={"examples": ["mistral"]})
    reset: Optional[List[StrictStr]] = Field(default=None, description="Fields to un-set, so they go back to tracking the platform default. Only the nullable fields can be reset; anything else answers `400`. Applied after the rest of the body.", json_schema_extra={"examples": [["rerankTopK", "spirit"]]})
    __properties: ClassVar[List[str]] = ["name", "description", "topK", "rerank", "rerankTopK", "context", "behaviour", "spirit", "useHistory", "historySize", "thinkingMode", "temperature", "rerankModel", "baseModel", "reset"]

    @field_validator('thinking_mode')
    def thinking_mode_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['LOW', 'HIGH']):
            raise ValueError("must be one of enum values ('LOW', 'HIGH')")
        return value

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of AgentUpdateRequest from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of AgentUpdateRequest from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "name": obj.get("name"),
            "description": obj.get("description"),
            "topK": obj.get("topK"),
            "rerank": obj.get("rerank"),
            "rerankTopK": obj.get("rerankTopK"),
            "context": obj.get("context"),
            "behaviour": obj.get("behaviour"),
            "spirit": obj.get("spirit"),
            "useHistory": obj.get("useHistory"),
            "historySize": obj.get("historySize"),
            "thinkingMode": obj.get("thinkingMode"),
            "temperature": obj.get("temperature"),
            "rerankModel": obj.get("rerankModel"),
            "baseModel": obj.get("baseModel"),
            "reset": obj.get("reset")
        })
        return _obj



# coding: utf-8

"""
    Verbatim AI — GenAI Backend API

      ## Concepts API of the **Verbatim AI** Retrieval-Augmented-Generation (RAG) platform is built over 5 domains: - **Corpus** — a knowledge base. Holds documents, threads, and is bound to an embedding model and a summary LLM. - **Document** — a file ingested into a corpus (PDF, DOCX, HTML…). - **Chunk** — one embeddable piece of a document, produced by ingestion. The unit retrieval actually returns. - **Thread** — a conversation thread bound to one or more corpora. - **Post** — a single user query or system answer inside a thread. Answers reference attachments (the chunks used as context).  ## Authentication Two authentication methods are accepted on endpoints:  | Method | Header | Allowed HTTP methods | Use case | |--------|--------|----------------------|----------| | **JWT Bearer** | `Authorization: Bearer <jwt>` | All | Server-to-server calls with your RSA-signed JWT | | **Access Token** | `X-Access-Token: <token>` | **Defined by the scope of the token** | Short-lived tokens issued by `POST /v1/access-token/` |  ## API status Get a fresh status from our [API Status dashboard](https://verbatim-ai.openstatus.dev/). Events, maintenance schedules and incidents will be reported in this page.  ## Conventions - **Pagination** — list endpoints accept `pageSize` (default `25`) and `pageIndex` (default `0`). - **IDs** — all resource identifiers are UUIDv4 strings. - **Timestamps** — ISO-8601 (`2026-04-23T04:06:51Z`). - **Errors** — non-2xx responses return a JSON body matching the `Error` schema. --- 

    The version of the OpenAPI document: v1
    Contact: contact@verbatim-ai.com
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from datetime import datetime
from pydantic import BaseModel, ConfigDict, Field, StrictInt
from typing import Any, ClassVar, Dict, List, Optional
from verbatim_client.models.usage_delta import UsageDelta
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class UsageBucket(BaseModel):
    """
    Metrics for one calendar bucket of the series (UTC). Every bucket is complete; the one currently in progress is not reported.
    """ # noqa: E501
    var_from: datetime = Field(description="Inclusive start of the bucket (ISO-8601, UTC).", alias="from", json_schema_extra={"examples": ["2026-07-25T00:00:00Z"]})
    to: datetime = Field(description="Exclusive end of the bucket (ISO-8601, UTC). Equal to the `from` of the next bucket.", json_schema_extra={"examples": ["2026-07-26T00:00:00Z"]})
    tokens: StrictInt = Field(description="Tokens produced inside this bucket. At organization and user scope, posts **and** documents; at corpus scope, posts only.", json_schema_extra={"examples": [410]})
    corpora: Optional[UsageDelta] = Field(default=None, description="Corpus deltas for this bucket. Populated at organization scope only; `null` at corpus and user scopes, as it is at the top level.")
    sessions: UsageDelta = Field(description="Session deltas for this bucket.")
    posts: UsageDelta = Field(description="Post deltas for this bucket.")
    storage: UsageDelta = Field(description="Storage deltas for this bucket, in **bytes** — not item counts.")
    __properties: ClassVar[List[str]] = ["from", "to", "tokens", "corpora", "sessions", "posts", "storage"]

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of UsageBucket from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of corpora
        if self.corpora:
            _dict['corpora'] = self.corpora.to_dict()
        # override the default output from pydantic by calling `to_dict()` of sessions
        if self.sessions:
            _dict['sessions'] = self.sessions.to_dict()
        # override the default output from pydantic by calling `to_dict()` of posts
        if self.posts:
            _dict['posts'] = self.posts.to_dict()
        # override the default output from pydantic by calling `to_dict()` of storage
        if self.storage:
            _dict['storage'] = self.storage.to_dict()
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of UsageBucket from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "from": obj.get("from"),
            "to": obj.get("to"),
            "tokens": obj.get("tokens"),
            "corpora": UsageDelta.from_dict(obj["corpora"]) if obj.get("corpora") is not None else None,
            "sessions": UsageDelta.from_dict(obj["sessions"]) if obj.get("sessions") is not None else None,
            "posts": UsageDelta.from_dict(obj["posts"]) if obj.get("posts") is not None else None,
            "storage": UsageDelta.from_dict(obj["storage"]) if obj.get("storage") is not None else None
        })
        return _obj


